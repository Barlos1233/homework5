[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "graphs_cviramon"
version = "0.1.0"
description = "Graph algorithms including Dijkstra's shortest path"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]